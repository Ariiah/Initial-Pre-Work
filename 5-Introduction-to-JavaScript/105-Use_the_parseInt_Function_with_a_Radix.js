// Adding the radix parameter gives the option to show if the number exsists in the particular numeric system stated.

function convertToInteger(str) {
  return parseInt(str, 2)
}

convertToInteger("10011");
