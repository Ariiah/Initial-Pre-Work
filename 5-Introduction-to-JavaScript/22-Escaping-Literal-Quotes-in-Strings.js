// To prevent from the code not processing quotes within a string literal, a backslash is used before the quote at the beginning and before the last quotation mark.

var myStr = "I am a \"double quoted\" string inside \"double quotes\".";
