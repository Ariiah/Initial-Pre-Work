// When calling parameters, it is important to place them in the exact way it was stated when the fucntion was first written.

// Example
function ourFunctionWithArgs(a, b) {
  console.log(a - b);
}
ourFunctionWithArgs(10, 5); // Outputs 5

// Only change code below this line.

function functionWithArgs(c, d) {
  console.log(c + d);
}
functionWithArgs(1, 2);
