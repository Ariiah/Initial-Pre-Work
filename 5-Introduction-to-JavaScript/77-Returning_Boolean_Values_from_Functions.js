// The shorthand way to use a boolean value is to strictly compare or use greater than or equal to the arguments.

function isLess(a, b) {
  // Fix this code
  return a <= b
}

// Change these values to test
isLess(10, 15);
