// The Math.random() function is a more feasible way to randomize numbers. It's better than assigning an array with random numbers where there's only a set amount.

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
