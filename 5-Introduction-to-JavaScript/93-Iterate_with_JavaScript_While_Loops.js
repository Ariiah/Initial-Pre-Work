// While loops are a good way to prevent from copying more code than nessessary. Although, if not stated correctly, it will get stick in an infinate loop.

// Setup
var myArray = [];

// Only change code below this line.
var i = 0
while (i < 5) {
  myArray.push(i);
  i++;
}
