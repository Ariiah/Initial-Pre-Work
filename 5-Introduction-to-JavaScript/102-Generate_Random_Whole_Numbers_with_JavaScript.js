// The Math.floor() only generates numbers between 0 and 1 by default. In order use higher numbers, multiplying it by the number you want will accomplish this.

var randomNumberBetween0and19 = Math.floor(Math.random() * 20);

function randomWholeNum() {

  // Only change code below this line.

  return Math.floor(Math.random() * 10);
}
